<!DOCTYPE html>
html(lang="en")
    head
        meta(charset="UTF-8")
        link(rel='stylesheet', href='/css/style.css')
        meta(name="viewport", content="width=device-width, initial-scale=1.0")
        title #{title}
    body
      main
        block header
           header.header 
             h1 #{title}
        block content
//block keyword allows us to extend template through inheritence